name: coronavirus
description: Coronavirus informacion

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 1.0.0+3

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_widget_from_html_core: ^0.3.2+1
  flutter_launcher_icons: "^0.7.3"
  firebase_admob: ^0.9.1+3

flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "images/icon_rojo.jpg"
  adaptive_icon_background: "images/icon_rojo.jpg"


# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - images/20651.jpg
    - images/queeselcoronavirus.jpg
    - images/informate_prevencion.jpg
    - images/quienes_padeceran.jpg
    - images/prevencion3.jpg
    - images/sintomas.jpg
    - images/prevencion1.png
    - images/probabilidad.jpg
    - images/quees3.jpg
    - images/coronavirus2019.jpg
    - images/prevencion4.jpg
    - images/prevencion5.jpg
    - images/proteccion.jpg
    - images/medicamentos.jpg
    - images/origen_coronavirus.jpg
    - images/paquetes_de_china.jpg
    - images/remedios.jpg
    - images/tratamiento1.png
    - images/imagen_coronavirus.jpg
    - images/que_es2.jpg
    - images/probabilidad.jpg
    - images/como_colocarse_una_mascarilla.jpg
    - images/como_colocarse_una_mascarilla2.jpg
    - images/como_colocarse_una_mascarilla3.jpg
    - images/como_colocarse_una_mascarilla4.jpg
    - images/cuando_votar_mascarilla.jpg
    - images/cuando_usar_mascarilla.jpg
    - images/prevencion2.png
    - images/infografiacoronavirus.png
    - images/vidacovid19.jpg
  #  - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware.

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
